# Legacy

Text-based RPG / interactive fiction game set in an ancient library.

## Tech Stack

- Nuxt 4, Vue 3
- Vintage parchment/wood themed UI

## Commands

- `npm install` — install dependencies
- `npm run dev` — dev server on localhost:3000
- `npm run build` — production build
